info:
  name: 05_signUp a user
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8000/auth/signup
  body:
    type: json
    data: |-
      {
        "username": "batman",
        "email": "batman@gmail.com",
        "password": "password",
        "is_staff": true,
        "is_active": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
