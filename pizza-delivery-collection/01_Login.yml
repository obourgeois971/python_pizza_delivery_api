info:
  name: 01_Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/auth/login
  body:
    type: json
    data: |-
      {
        "username":"johndoe",
        "password":"password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
